
syntax = "proto3";

import "dellve_types.proto";

////////////////////////////////////////////////////////////////////////////////////////////////////
// 	DELLveBackend - back-end service for benchmark's read-time data
////////////////////////////////////////////////////////////////////////////////////////////////////

service Backend {
	rpc consumeMetricStream(stream MetricStreamData) returns (stream MetricStreamCommand);
	rpc consumeLogStream(stream Log) returns (Status);
}